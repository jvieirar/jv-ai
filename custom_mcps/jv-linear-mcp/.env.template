# Linear personal API key (Settings → Account → API → Create personal API key)
# Format: lin_api_…
LINEAR_API_KEY=

# Optional — Linear GraphQL endpoint (defaults to https://api.linear.app/graphql)
# LINEAR_API_URL=https://api.linear.app/graphql

# Optional — directory for downloaded attachments (defaults to /tmp/jv-linear-mcp)
# LINEAR_DOWNLOAD_DIR=/tmp/jv-linear-mcp
