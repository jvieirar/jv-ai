{
  "name": "jv-linear-mcp",
  "version": "0.1.0",
  "description": "Trimmed Linear MCP server. Hand-written GraphQL queries + lifecycle tools tuned to the `jv-linear` agent skill.",
  "private": true,
  "type": "module",
  "engines": {
    "bun": ">=1.0.0"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.24.3",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "typescript": "^5.9.3"
  },
  "bin": {
    "jv-linear-mcp": "src/index.ts"
  }
}
